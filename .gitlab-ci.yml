image: node:20

stages:
  - setup
  - validate
  - test:utils
  - test:commands
  - test:scripts
  - test:integration

# Setup stage - Install dependencies
setup-dependencies:
  stage: setup
  only:
    - merge_requests
    - main
    - master
  variables:
    DISCORD_TOKEN: 'ci-test-token'
    CLIENT_ID: 'ci-test-client-id'
  script:
    - npm ci
  cache:
    paths:
      - node_modules/
    key: ${CI_COMMIT_REF_SLUG}
    policy: push

# Validate stage - Code quality checks
validate-code:
  stage: validate
  only:
    - merge_requests
    - main
    - master
  variables:
    DISCORD_TOKEN: 'ci-test-token'
    CLIENT_ID: 'ci-test-client-id'
  cache:
    paths:
      - node_modules/
    key: ${CI_COMMIT_REF_SLUG}
    policy: pull
  script:
    - npm run validate
  dependencies:
    - setup-dependencies

# Test stage - Utility functions
test-utils:
  stage: test:utils
  only:
    - merge_requests
    - main
    - master
  variables:
    DISCORD_TOKEN: 'ci-test-token'
    CLIENT_ID: 'ci-test-client-id'
  cache:
    paths:
      - node_modules/
    key: ${CI_COMMIT_REF_SLUG}
    policy: pull
  script:
    - set +e
    - npx cross-env R2_ACCOUNT_ID= R2_ACCESS_KEY_ID= R2_SECRET_ACCESS_KEY= R2_BUCKET_NAME= node --test --test-reporter tap test/utils/ > test-utils.tap
    - TEST_EXIT_CODE=$?
    - if ! npx tap-junit test-utils.tap -o test-utils.xml 2>/dev/null; then echo '<?xml version="1.0" encoding="UTF-8"?><testsuites></testsuites>' > test-utils.xml; fi
    - exit $TEST_EXIT_CODE
  artifacts:
    when: always
    paths:
      - test-utils.xml
    reports:
      junit: test-utils.xml
  dependencies:
    - setup-dependencies

# Test stage - Command handlers
test-commands:
  stage: test:commands
  only:
    - merge_requests
    - main
    - master
  variables:
    DISCORD_TOKEN: 'ci-test-token'
    CLIENT_ID: 'ci-test-client-id'
  cache:
    paths:
      - node_modules/
    key: ${CI_COMMIT_REF_SLUG}
    policy: pull
  script:
    - set +e
    - npx cross-env R2_ACCOUNT_ID= R2_ACCESS_KEY_ID= R2_SECRET_ACCESS_KEY= R2_BUCKET_NAME= node --test --test-reporter tap test/commands/ > test-commands.tap
    - TEST_EXIT_CODE=$?
    - if ! npx tap-junit test-commands.tap -o test-commands.xml 2>/dev/null; then echo '<?xml version="1.0" encoding="UTF-8"?><testsuites></testsuites>' > test-commands.xml; fi
    - exit $TEST_EXIT_CODE
  artifacts:
    when: always
    paths:
      - test-commands.xml
    reports:
      junit: test-commands.xml
  dependencies:
    - setup-dependencies

# Test stage - Scripts
test-scripts:
  stage: test:scripts
  only:
    - merge_requests
    - main
    - master
  variables:
    DISCORD_TOKEN: 'ci-test-token'
    CLIENT_ID: 'ci-test-client-id'
  cache:
    paths:
      - node_modules/
    key: ${CI_COMMIT_REF_SLUG}
    policy: pull
  script:
    - set +e
    - npx cross-env R2_ACCOUNT_ID= R2_ACCESS_KEY_ID= R2_SECRET_ACCESS_KEY= R2_BUCKET_NAME= node --test --test-reporter tap test/scripts/ > test-scripts.tap
    - TEST_EXIT_CODE=$?
    - if ! npx tap-junit test-scripts.tap -o test-scripts.xml 2>/dev/null; then echo '<?xml version="1.0" encoding="UTF-8"?><testsuites></testsuites>' > test-scripts.xml; fi
    - exit $TEST_EXIT_CODE
  artifacts:
    when: always
    paths:
      - test-scripts.xml
    reports:
      junit: test-scripts.xml
  dependencies:
    - setup-dependencies

# Test stage - Integration/server components
test-integration:
  stage: test:integration
  only:
    - merge_requests
    - main
    - master
  variables:
    DISCORD_TOKEN: 'ci-test-token'
    CLIENT_ID: 'ci-test-client-id'
  cache:
    paths:
      - node_modules/
    key: ${CI_COMMIT_REF_SLUG}
    policy: pull
  script:
    - set +e
    - npx cross-env R2_ACCOUNT_ID= R2_ACCESS_KEY_ID= R2_SECRET_ACCESS_KEY= R2_BUCKET_NAME= node --test --test-reporter tap test/webui-server-*.test.js test/docker-security.test.js > test-integration.tap
    - TEST_EXIT_CODE=$?
    - if ! npx tap-junit test-integration.tap -o test-integration.xml 2>/dev/null; then echo '<?xml version="1.0" encoding="UTF-8"?><testsuites></testsuites>' > test-integration.xml; fi
    - exit $TEST_EXIT_CODE
  artifacts:
    when: always
    paths:
      - test-integration.xml
    reports:
      junit: test-integration.xml
  dependencies:
    - setup-dependencies
